/* TODO: Layout of weather components. */
import { FC, ReactElement } from 'react';

import { Numeric } from '../components';

const WeatherView: FC = (): ReactElement => {
  return (
    <>
      <Numeric />
    </>
  );
};

export default WeatherView;
